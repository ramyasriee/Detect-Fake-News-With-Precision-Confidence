{
  "version": 2,
  "buildCommand": "cd frontend_extracted && npm ci && npm run build",
  "outputDirectory": "frontend_extracted/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
